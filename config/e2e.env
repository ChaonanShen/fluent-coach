# Dedicated Playwright e2e configuration. Keep this deterministic and
# independent from the developer .env used by the normal app server.
LLM_PROVIDER=fake
ASR_PROVIDER=fake
PRON_PROVIDER=mock
TTS_PROVIDER=browser
APP_DB_PATH=.local/e2e/speaking_coach.sqlite
APP_AUDIO_DIR=.local/e2e/audio
